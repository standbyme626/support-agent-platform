# 升级5任务分解与执行清单（S1-S4）

> 主规范来源：`升级5.md`  
> 执行目标：提效、解决当前语义问题、并让 Codex 可安全升级  
> 约束：`升级5.md` 只读，不在本清单中直接改动

---

## 0. 使用方式

1. 严格按 `S1 -> S2 -> S3 -> S4` 执行。
2. 每阶段完成后输出：改动文件、测试结果、风险点。
3. 未完成当前阶段退出标准，不得进入下一阶段。

---

## 1. 文件清单（本轮最小 10 文件）

1. `API_CONTRACT_V2.md`
2. `app/domain/conversation/conversation_state.py`
3. `app/domain/ticket/ticket_workflow_state.py`
4. `app/application/session_service.py`
5. `app/domain/ticket/ticket_api.py`
6. `app/application/intake_service.py`
7. `app/application/collab_service.py`
8. `app/graph_runtime/intake_graph.py`
9. `app/graph_runtime/collab_graph.py`
10. `app/agents/deep/ticket_investigation_agent.py`

---

## 2. S1-S4 执行清单

## S1：仅创建目录和空壳

| ID | 任务 | 产出 | 验收 |
| --- | --- | --- | --- |
| S1-1 | 建立 `app/` 目录树 | 10个目标文件存在 | 文件可读、Python文件无语法错误 |
| S1-2 | 写最小占位头 | 每个文件有职责注释 | 不改变现有行为 |
| S1-3 | 建立阶段标记 | 在清单中登记已建文件 | 可复核 |

## S2：只填 4 个边界文件

| ID | 任务 | 产出 | 验收 |
| --- | --- | --- | --- |
| S2-1 | 编写 `API_CONTRACT_V2.md` | v2 路由、错误结构、兼容规则 | 明确新增 `customer_confirm/operator_close/session_end` |
| S2-2 | 落地 `conversation_state.py` | 会话状态模型 | 不含 ticket 生命周期字段 |
| S2-3 | 落地 `ticket_workflow_state.py` | 工单状态模型 | 不含会话焦点字段 |
| S2-4 | 落地 `session_service.py` | 会话上下文服务 | 不直接变更 ticket 生命周期 |

## S3：只填 ticket 动作语义

| ID | 任务 | 产出 | 验收 |
| --- | --- | --- | --- |
| S3-1 | 在 `ticket_api.py` 增加 `customer_confirm` | 新动作与事件 | 仅允许从 `resolved` 进入确认关闭路径 |
| S3-2 | 在 `ticket_api.py` 增加 `operator_close` | 强制关单动作 | 审计字段完整 |
| S3-3 | 在 `ticket_api.py` 增加 `end_session` 协作入口 | 结束会话动作 | 不等于关闭工单 |
| S3-4 | 兼容 `/close` | 受控路由策略 | 禁止继续“万能动作” |

## S4：最后填 graph + deep agent

| ID | 任务 | 产出 | 验收 |
| --- | --- | --- | --- |
| S4-1 | 编写 `intake_graph.py` | 最小可运行编排入口 | 不吞并 domain 规则 |
| S4-2 | 编写 `ticket_investigation_agent.py` | 分析型 agent | 默认仅建议，不自动高风险执行 |
| S4-3 | 串联最小调用路径 | 能从入口到结果 | trace 可观测 |

---

## 3. 阶段门（必须）

1. S1 -> S2：10 文件存在，且未引入行为变更。
2. S2 -> S3：状态边界与 v2 契约已确定。
3. S3 -> S4：`/close` 语义拆分完成并通过动作测试。
4. S4 结项：graph/agent 接入后，既有主链路无回退。

---

## 4. 最小回归建议

1. `python -m pytest tests/integration/test_session_api.py -q`
2. `python -m pytest tests/integration/test_ticket_actions_api.py -q`
3. `python -m pytest tests/workflow/test_case_collab_workflow.py -q`
4. `python -m pytest tests/regression/test_case_collab_state_machine_regression.py -q`

