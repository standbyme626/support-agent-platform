# 升级5-3 文档现状核查（2026-03-14）

核查范围：仅核对 `升级5-3.md` 中与当前仓库状态相关的关键断言。

## 结论摘要

- 属实：6 项
- 不属实（已过时）：1 项
- 总体判断：文档大方向基本符合当前仓库现状，但“后端缺少 operator/dispatch copilot 路由”这一条已经过时。

## 逐条核查

1. 断言：仓库仍定义为 `workflow-first, agent-assisted`。
- 结论：属实（source-verified）
- 证据：`README.md` 明确写明该定位。
- 位置：`README.md:5`

2. 断言：业务主干仍在 `core/ + workflows/ + scripts/ops_api_server.py`。
- 结论：属实（source-verified）
- 证据：README 明确写出主干边界。
- 位置：`README.md:41`

3. 断言：运行时依赖仍只有 `httpx`、`pydantic`，未引入 `langgraph/langchain/deepagents`。
- 结论：属实（source-verified）
- 证据：`pyproject.toml` 的 `[project].dependencies` 仅包含 `httpx` 与 `pydantic`；代码内未检索到 `langgraph/deepagents` Python 引用。
- 位置：`pyproject.toml:10-13`

4. 断言：`API_CONTRACT_V2.md` 仍处于 `draft`、`upgrade5-s2`。
- 结论：属实（source-verified）
- 证据：文档头部状态字段未变更。
- 位置：`API_CONTRACT_V2.md:1-5`

5. 断言：后端只有 `COPILOT_TICKET_QUERY_RE`，没有 operator/dispatch copilot 路由。
- 结论：不属实（source-verified）
- 证据：后端已实现 `POST /api/copilot/operator/query` 与 `POST /api/copilot/dispatch/query` 路由。
- 位置：`scripts/ops_api_server.py:2365`, `scripts/ops_api_server.py:2405`

6. 断言：前端会请求 operator/dispatch copilot 接口。
- 结论：属实（source-verified）
- 证据：前端 API 客户端存在 `queryOperatorCopilot` 与 `queryDispatchCopilot`。
- 位置：`web_console/lib/api/tickets.ts:452-487`

7. 断言：`useTicketDetail` 里三路 copilot 是并发请求。
- 结论：属实（source-verified）
- 证据：使用 `Promise.all` 并发请求 ticket/operator/dispatch 三路。
- 位置：`web_console/lib/hooks/useTicketDetail.ts:146-150`

8. 断言：WeCom adapter 当前会把 outbound 发回 `session_id`（不能天然跨群）。
- 结论：属实（source-verified）
- 证据：入站 `conversation_id` 取 `session_id`；出站 `touser` 直接使用 `envelope.session_id`。
- 位置：`channel_adapters/wecom_adapter/adapter.py:133`, `channel_adapters/wecom_adapter/adapter.py:148`

## 备注

- 文档中的“断点一”建议需要更新为：
  - 后端路由已存在，但仍需核验其是否达到“真 Agent runtime”能力目标（而非仅接口可用）。
